from setuptools import setup, find_packages

from moduletracker import moduletracker

setup(
    name='ImportSearch',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        
    ],
)


